# Harbour Line Advisory

Minimal static landing page for Harbour Line Advisory.

## Typeform connection

In `index.html`, replace the `href` on the **Start the operating brief** link with the published Typeform URL.

The site uses plain HTML and CSS and can be deployed through GitHub Pages, Cloudflare Pages, Netlify, or any static host.
